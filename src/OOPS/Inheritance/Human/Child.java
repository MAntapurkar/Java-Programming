package OOPS.Inheritance.Human;

/**
 * Child class inherits from Parent (Level 3 of Inheritance).
 * Because Parent inherits from GrandParent, Child inherits from BOTH.
 * This is called MULTILEVEL INHERITANCE.
 */
public class Child extends Parent {
    // Child inherits name, age, hasSuperpowers, and all getters/setters 
    // from GrandParent through Parent.
}
